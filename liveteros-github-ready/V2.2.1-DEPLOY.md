# V2.2.1 - cache-safe build

This build uses unique frontend asset filenames (`app-v221.js`, `styles-v221.css`, `sw-v221.js`) so an older service worker/browser cache cannot silently mix V1/V2.1 assets with the current UI.

Visible build badge: `V2.2.1` in the header.

Verified in source:
- No PENAL URU / PENAL RIVAL buttons exist in Home.
- Penalties are available inside PÉRDIDA / FIN.
- There is no automatic question asking whether a sequence entered and exited the 22.
- `+ ENTRÓ 22` remains the manual exception button.
- `CAMBIAR POSESIÓN` remains visible as a state correction tool.
